import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.developergame.kuri',
  appName: 'kuri',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
